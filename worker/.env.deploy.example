# デプロイ用リソースID のテンプレート。
# このファイルをコピーして worker/.env.deploy を作り、実値を入れる（.env.deploy は git 管理外）。
#   cp .env.deploy.example .env.deploy
# CI では GitHub の Variables/Secrets から同名の環境変数で渡す。
D1_DATABASE_ID=your-d1-database-id
KV_RL_ID=your-kv-namespace-id
